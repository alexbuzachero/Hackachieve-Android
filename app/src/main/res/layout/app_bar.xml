<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="80dp"
    android:background="@color/HeaderBackground"
    android:gravity="top"
    android:outlineAmbientShadowColor="@color/monocolorPallete04"
    android:outlineSpotShadowColor="@color/monocolorPallete04"
    android:paddingTop="28dp"
    app:popupTheme="@style/CustomPopupStyle"
    app:subtitleTextColor="@color/monocolorPallete04"
    app:theme="@style/CustomToolbarStyle"
    app:titleTextColor="@color/monocolorPallete04">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/pageTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Main Goals"
            android:textColor="@color/monocolorPallete04"
            android:textSize="22sp" />

        <ImageView
            android:id="@+id/avatar"
            android:layout_width="wrap_content"
            android:layout_height="35dp"
            android:layout_marginLeft="160dp"
            android:contentDescription="Avatar"
            android:src="@drawable/avatar_johnsnow_border2x" />
    </LinearLayout>
</android.support.v7.widget.Toolbar>
